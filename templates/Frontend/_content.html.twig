	{{ article.content|raw }}
	{# Outputs the content of the article without escaping any HTML tags #}

	{{ category.title|raw }} 
	{# Outputs the title of the category without escaping any HTML tags #}
